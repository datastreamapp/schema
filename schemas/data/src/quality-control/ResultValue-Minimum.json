{
  "title": "Result Minimum Measure",
  "description": "",
  "errorMessage": "selected unit cannot have a measure below zero",
  "if": {
    "properties": {
      "ResultUnit": {
        "enum": ["mg/L", "ppm"]
      }
    },
    "required": ["ResultUnit"]
  },
  "then": {
    "properties": {
      "ResultValue": {
        "minimum": 0
      }
    },
    "required": ["ResultValue"]
  }
}
