{
  "title": "Unit Should be `None`",
  "description": "Characteristics that require None as unit.",
  "errorMessage": "The unit for the CharacteristicName should be None",
  "if": {
    "properties": {
      "CharacteristicName": {
        "enum": [
          "pH",
          "pH, lab",
          "pH, saturated",
          "Langelier Saturation Index"
        ]
      }
    },
    "required": ["CharacteristicName","ResultDetectionQuantitationLimitMeasure"]
  },
  "then": {
    "properties": {
      "ResultDetectionQuantitationLimitUnit": {
        "enum": ["None"]
      }
    },
    "required": ["ResultDetectionQuantitationLimitUnit"]
  }
}
