{
  "$id": "https://datastream.org/schema/data/quality-control/ResultUnit-Salinity.json",
  "title": "ResultUnit-Salinity",
  "description": "ResultUnit that require special unit notation.",
  "errorMessage": "ResultUnit for CharacteristicName Salinity should be ppth (parts per thousand), ppm (parts per million) or PSU (practical salinity unit)",
  "if": {
    "properties": {
      "CharacteristicName": {
        "enum": ["Salinity"]
      }
    },
    "required": ["CharacteristicName", "ResultUnit"]
  },
  "then": {
    "properties": {
      "ResultUnit": {
        "enum": ["ppth", "ppm", "PSU", "None"]
      }
    },
    "required": ["ResultUnit"]
  }
}
