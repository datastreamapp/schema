{
  "errorMessage": "ResultDetectionCondition of non-detect should not have ResultDetectionQuantitationLimit columns set",
  "if": {
    "properties": {
      "ResultDetectionCondition": {
        "enum": [
          "Detected Not Quantified",
          "Not Detected"
        ]
      }
    },
    "required": [
      "ResultDetectionCondition"
    ]
  },
  "then": {
    "properties": {
      "ResultDetectionQuantitationLimitType": {
        "enum": [null, ""]
      },
      "ResultDetectionQuantitationLimitMeasure": {
        "enum": [null, ""]
      },
      "ResultDetectionQuantitationLimitUnit": {
        "enum": [null, ""]
      }
    }
  }
}
