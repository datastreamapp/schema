{
  "title": "ResultDetectionCondition-ResultDetectionQuantitationLimit-not-detect",
  "description": "When ResultDetectionCondition is non-detect, `ResultDetectionQuantitationLimit*` should not be set.",
  "errorMessage": "requires ResultDetectionQuantitationLimit columns to be empty when ResultDetectionCondition is non-detect",
  "if": {
    "properties": {
      "ResultDetectionCondition": {
        "enum": [
          "Detected Not Quantified",
          "Not Detected"
        ]
      }
    },
    "required": [
      "ResultDetectionCondition"
    ]
  },
  "then": {
    "properties": {
      "ResultDetectionQuantitationLimitType": {
        "enum": [null, ""]
      },
      "ResultDetectionQuantitationLimitMeasure": {
        "enum": [null, ""]
      },
      "ResultDetectionQuantitationLimitUnit": {
        "enum": [null, ""]
      }
    }
  }
}
