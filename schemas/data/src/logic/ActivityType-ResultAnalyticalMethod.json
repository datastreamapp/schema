{
  "title": "ActivityType-ResultAnalyticalMethod",
  "description": "When `ActivityType` starts with `Sample*`, `LaboratoryName` and `ResultAnalyticalMethod*` columns are need to provide necessary metadata.",
  "errorMessage": "requires LaboratoryName and ResultAnalyticalMethod columns when ActivityType starts with `Sample`",
  "if": {
    "properties": {
      "ActivityType": {
        "pattern": "^Sample.*$"
      }
    },
    "required": ["ActivityType"]
  },
  "then": {
    "anyOf": [
      {
        "dependencies": {
          "ActivityType": [
            "LaboratoryName",
            "ResultAnalyticalMethodID",
            "ResultAnalyticalMethodContext"
          ],
          "ResultAnalyticalMethodID": ["ResultAnalyticalMethodContext"],
          "ResultAnalyticalMethodContext": ["ResultAnalyticalMethodID"]
        }
      },
      {
        "dependencies": {
          "ActivityType": ["LaboratoryName", "ResultAnalyticalMethodName"]
        }
      }
    ]
  }
}
