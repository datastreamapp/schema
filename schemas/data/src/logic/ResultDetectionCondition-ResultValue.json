{
  "title": "ResultDetectionCondition-ResultValue",
  "description": "Either ResultValue or ResultDetectionCondition should be set.",
  "errorMessage": "requires only one of ResultValue or ResultDetectionCondition to be set",
  "oneOf": [
    {
      "properties": {
        "ResultDetectionCondition": {
          "enum": [null,""]
        }
      },
      "required": [
        "ResultValue"
      ]
    },
    {
      "properties": {
        "ResultValue": {
          "enum": [null,""]
        }
      },
      "required": [
        "ResultDetectionCondition"
      ]
    }
  ]
}