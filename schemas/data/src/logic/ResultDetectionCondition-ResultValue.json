{
  "errorMessage": "only one of ResultValue or ResultDetectionCondition can be set at once",
  "oneOf": [
    {
      "properties": {
        "ResultDetectionCondition": {
          "enum": [null,""]
        }
      },
      "required": [
        "ResultValue"
      ]
    },
    {
      "properties": {
        "ResultValue": {
          "enum": [null,""]
        }
      },
      "required": [
        "ResultDetectionCondition"
      ]
    }
  ]
}